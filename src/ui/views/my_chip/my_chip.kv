<MyChip>:
    pos_hint: {"center_x": .5, "center_y": .5}
    md_bg_color: app.theme_cls.primary_color if self.active else "white"
    line_color: "silver"
    text: self.entity.title if self.entity else '?'
