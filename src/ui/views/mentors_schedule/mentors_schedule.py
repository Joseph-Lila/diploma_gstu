from kivymd.uix.card import MDCard

from src import config


class MentorsSchedule(MDCard):
    def fill_grid_with_records(self, records, pairs_time=config.get_pairs_time()):
        # create pairs time block

        # create other blocks for each mentor
        raise NotImplementedError
